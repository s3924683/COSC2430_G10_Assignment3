<!DOCTYPE html>
<html lang="en">

<head>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="/css/userSignup.css" />
  <title>Shipper Sign-up</title>
</head>

<body>
  <div class="customer">
    <nav class="nav nav-pills nav-fill">
      <a class="nav-link bg-light text-dark rounded-0 rounded-top active" aria-current="page"
        href="/shipper/signup">Shipper Sign Up</a>
      <a class="nav-link text-light" href="/shipper/signin/">Shipper Login</a>
    </nav>
    <hr size="5px" />
    <div class="login">
      <h1 class="text-center">Shipper Sign Up</h1>
      <% if (message) { %>
        <% if (messageType==='success' ) { %>
          <p class="message message-success">
            <%= message %>
          </p>
          <% } else if (messageType==='error' ) { %>
            <p class="message message-error">
              <%= message %>
            </p>
            <% } %>
              <% } %>
                <form id="signup-form" method="POST" action="/shipper/signup">
                  <div class="form-field">
                    <label class="form-label" for="username">Username</label>
                    <input class="form-control" name="username" type="text" id="username" required />
                    <small></small>
                  </div>
                  <div class="form-field">
                    <label class="form-label" for="password">Password</label>
                    <input class="form-control" name="password" type="password" id="password" required />
                    <small></small>
                  </div>
                  <div class="form-group">
                    <label for="distributionHub">Distribution Hub</label>
                    <select id="distributionHub" name="distributionHub" class="form-control">
                      <option value="Ho Chi Minh">Ho Chi Minh</option>
                      <option value="Ha Noi">Ha Noi</option>
                      <option value="Da Nang">Da Nang</option>
                    </select>
                  </div>
                  <input type="hidden" name="isShipper" value="true">
                  <input class="btn w-100" type="submit" name="submit" value="SIGN UP" />
                  <a href="/user/signup/">User Sign Up</a>
                  <a href="/vendor/signup/">Vendor Sign Up</a>
                </form>
    </div>
    <div class="text-center">
      <img id="i1" src="/assets/imgs/logo.gif" alt="Logo" />
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>